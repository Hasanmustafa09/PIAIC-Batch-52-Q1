// it contains Whitespace chracters `space`, `\t`tab & `\n` new line

let stringWithWhitespace: string = "   Hello\tWorld\n lets begin";

console.log(stringWithWhitespace);

//  trim() method removed the leading and trailing spaces and the newline character, leaving only the spaces within the string.

console.log(stringWithWhitespace.trim());
